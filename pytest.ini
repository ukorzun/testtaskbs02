[pytest]
markers =
    smoke: fast critical checks
    regression: broader functional checks
    contract: schema/contract validations
    resilience: retries/timeouts/rate-limit behavior
    data: data-driven/boundary/fuzz-like checks
    integration: integration tests that require external services (e.g., RabbitMQ)

testpaths = tests
